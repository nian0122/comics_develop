export interface ChapterRef {
  seriesName: string;
  seasonName: string;
  chapterName: string;
}

export interface ChapterFiles {
  files: string[];
}

export interface SeriesResponse {
  items: string[];
}
